export interface IHomework{//I can explain what is this, but there isn't any information. Other inerfaces are the same. Nothing to tell.
	subject:string,
	whenGiven:string,
	whenPass:string,
	textDescription:string,
	fileName:string,
	source:string;
}
